version: 2.1

orbs:
  win: circleci/windows@5.0.0

jobs:
  unit-tests:
    executor:
      name: win/server-2022
      shell: powershell.exe
    steps:
      - run: Invoke-Item "C:\Program Files\Internet Explorer\iexplore.exe"
 
workflows:
  integration-tests:
    jobs:
      - unit-tests